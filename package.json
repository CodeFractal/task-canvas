{
  "scripts": {
    "watch": "esbuild --bundle src/Startup.ts --outfile=static/bundled/app.js --sourcemap --watch",
    "build": "esbuild --bundle src/Startup.ts --outfile=static/bundled/app.js --sourcemap --minify"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
